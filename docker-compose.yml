version: '3.8'

services:
  macsystem-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: macsystem-mcp
    restart: unless-stopped
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./tmp:/tmp
